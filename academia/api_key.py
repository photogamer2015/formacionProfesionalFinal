# ─────────────────────────────────────────────────────────────────────────
# MercyBot opera en modo LOCAL (sin API de ChatGPT/OpenAI).
# La clave embebida que existía aquí fue eliminada por seguridad y porque la
# integración con OpenAI está desactivada. Este archivo se conserva solo para
# no romper importaciones legadas; expone una clave vacía.
#
# Si en el futuro se desea reactivar la integración con OpenAI, define la
# clave mediante la variable de entorno OPENAI_API_KEY en el archivo .env
# (nunca la escribas directamente en el código ni la subas al repositorio).
# ─────────────────────────────────────────────────────────────────────────

OPENAI_API_KEY = ""
